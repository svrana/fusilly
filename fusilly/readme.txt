
each target must know which directory it resides in so the paths can be constructed correctly

deploy at root by default. i.e., head of directory structure must align with where the .git repo is,
not where the project build file is located.

